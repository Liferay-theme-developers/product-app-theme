#set ($fullScreenNavigation = $theme.getSetting("full-screen-navigation"))
#set ($socialMediaFacebook = $theme.getSetting("facebook-profile-visible"))
#set ($socialMediaTwitter = $theme.getSetting("twitter-profile-visible"))
#set ($socialMediaFacebookUrl = $theme.getSetting("facebook-profile-url"))
#set ($socialMediaTwitterUrl = $theme.getSetting("twitter-profile-url"))
#set ($retinaSiteLogo = $theme.getSetting("retina-site-logo"))

#if ($fullScreenNavigation == true)
	#set ($css_class = "$css_class full-screen-navigation")
#else
	#set ($nav_css_right = "navbar-right")
	#set ($nav_collapse = "navbar-collapse")
#end

#if ($retinaSiteLogo == true)
	#set ($company_logo_height = ($company_logo_height/2))
	#set ($company_logo_width = ($company_logo_width/2))
#end